[pytest]
testpaths = tests
addopts = --cov=backend/services --cov=packages --cov-report=term-missing --cov-report=xml
markers =
    unit: unit tests
    integration: tests that require running Docker containers or databases
    resilience: fault tolerance and chaos tests
pythonpath = .
